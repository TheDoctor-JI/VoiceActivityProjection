debug_time: True

max_instance_cnt: 1
context_size_sec: 10
step_size_sec: 0.12
frame_hz: 50
device: "cuda:0"
model_path: "example/VAP_3mmz3t0u_50Hz_ad20s_134-epoch9-val_2.56.pt"

occupying_channel_threshold_near: 0.75
interested_user_bin_pattern_near: [1,1,0,0] ## VAP bin pattern for the user. The system as well as other bins will be marginalized out
occupying_channel_threshold_far: 0.9
interested_user_bin_pattern_far: [0,0,1,1] ## VAP bin pattern for the user. The system as well as other bins will be marginalized out
thread_sleep_interval: 0.005
user_floor_latching_sec: 0.24
